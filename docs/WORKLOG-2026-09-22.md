# Worklog — 2026-09-22

## Scope

Physical Fedora 44 / GNOME Shell 50.5 workstation. This entry closes the day's Fedora-workstation work and records only results that were actually observed or already accepted in the repository.

## Plymouth offline-update localization — physical visual closure

A real Fedora offline-update cycle was observed on the physical workstation.

The Plymouth update screen rendered in Polish and visibly showed:

- `Instalowanie aktualizacji…`;
- `Nie należy wyłączać komputera`;
- translated percentage progress, with `Ukończono 13%` observed during the update.

This closes the final visual-validation gap that remained after the earlier technical validation of the rebuilt initramfs.

Previously verified technical state:

- Fedora's installed Plymouth catalog already contained the required Polish translations;
- the root cause of the English offline-update screen had been traced to missing Polish locale/gettext assets inside initramfs;
- the repository-managed dracut configuration persisted the required locale files, `/etc/locale.conf`, and `plymouth.mo`;
- the rebuilt initramfs increased only from roughly 156 MiB to 157 MiB;
- the system booted successfully with the rebuilt image;
- `systemctl --failed` reported zero failed units at the validation checkpoint.

With the 2026-09-22 real offline update, the chain is now complete:

```text
repository configuration        PASS
initramfs contents              PASS
boot after rebuild              PASS
systemd failed units            0
real offline-update execution   PASS
Polish Plymouth UI              PASS
```

## Documentation updates

Updated:

- `README.md` — Plymouth now explicitly recorded as visually confirmed on a real physical offline update;
- `PROJECT-STATUS.md` — current localization status updated with the 2026-09-22 physical observation;
- `docs/LOCALIZATION-STATUS.md` — removed the obsolete "visual confirmation pending" limitation and clarified the historical localization checkpoint versus the current full physical aggregate;
- `docs/README.md` — added this dated worklog to the documentation index.

## Current accepted workstation state at day close

Platform:

- Fedora 44;
- GNOME Shell 50.5;
- Wayland;
- physical Lenovo Legion 5 15ACH6H.

Current complete physical acceptance remains:

```text
PASS=256 WARN=0 FAIL=0 SKIP=0
VERIFY_RC=0
```

The latest accepted public repository baseline before this documentation closure was commit:

```text
4527277f91a54d04d62f9e45b71dc3933937aad8
```

## Remaining explicit limits

- LUKS full-disk encryption remains deliberately deferred and is not claimed as part of the current security baseline.
- VSCodium localization remains deliberately deferred and outside the reproducible localization claim.
- The historical clean-room restore result remains the Fedora 44 / GNOME 50.4 VM checkpoint; the current physical workstation is GNOME 50.5.

## Day-close status

**CLOSED.**

No additional Fedora configuration change is required from the Plymouth observation itself. Future Fedora/GNOME, kernel/NVIDIA, extension, localization, restore, or security-policy changes should continue through the documented repository validation and physical acceptance process.
