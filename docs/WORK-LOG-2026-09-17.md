# Work log — 2026-09-17

End-of-day record for the Fedora workstation setup project.

## Environment

- Fedora 44
- GNOME Shell 50.4
- Wayland
- Physical host: Lenovo Legion 5 15ACH6H

## GNOME extension and desired-state work

Today the GNOME extension set and repository desired state were brought to a clean, reproducible state.

Key results:

- completed the controlled GNOME 50 extension compatibility refresh;
- kept Dhruva as the canonical dock and removed the duplicate Dash2Dock Animated path;
- removed the incompatible/out-of-date Media Controls extension from the accepted set;
- removed Freon from the physical workstation and from desired state by design;
- removed NordVPN / gNordVPN-Local from the workstation desired state; Tailscale remains the supported overlay/VPN component;
- added Advanced Media Controller v31 / 6.5 to desired state after physical validation;
- refreshed extension inventory and retained version-aware drift detection.

## Network / firewalld correction

The physical verifier exposed a Wi-Fi firewalld-zone failure. Diagnostics showed that the NetworkManager profile already stored `public`, but firewalld itself was not running, so the active interface had no runtime zone.

After restoring firewalld operation and the active `public` assignment, the workstation returned to a clean zero-failure verification state. The repository continues to follow the active/default-route interface instead of assuming the global default zone.

## Polish localization work

The Polish-localization pass was completed and physically validated.

Finalized or revalidated today:

- GSConnect v72 completion and Shell gettext-domain integration;
- Tiling Shell v76 / 17.3 completion overlay;
- Just Perfection v37 localization;
- Spotlight v14 / 2026.11 controlled gettext integration;
- Space Bar v39 exact-version controlled localization;
- AppIndicator v64 five-entry completion overlay;
- Vitals v85 completion overlay and preference-page cleanup;
- ddterm v72 completion plus localized About metadata description;
- Advanced Media Controller v31 / 6.5 full Polish catalog.

Advanced Media Controller was the final new localization target. The repository now carries a complete 276-entry Polish gettext catalog pinned to the tested v31 / 6.5 build, with installer, verifier, file fingerprints, byte-for-byte `.mo` verification, and a runtime gettext smoke test requiring `General` to resolve to `Ogólne`.

All version-specific localization installers are wired into `scripts/install-localizations.sh`, and their dedicated checks are integrated into the main `scripts/verify.sh`.

## Final physical verification

The completed physical-host verification after the final localization integration reported:

```text
PASS=222 WARN=0 FAIL=0 SKIP=0
VERIFY_RC=0
```

This is the accepted end-of-day physical baseline for the current Fedora 44 / GNOME 50.4 desired state.

The historical clean-room VM result remains unchanged:

```text
PASS=147 WARN=0 FAIL=0 SKIP=8
```

## Repository / CI state

The final localization verifier integration and documentation updates passed the repository static validation workflow. `README.md`, `PROJECT-STATUS.md`, and `docs/LOCALIZATION-STATUS.md` were updated to record the accepted `PASS=222 WARN=0 FAIL=0 SKIP=0` physical result.

## End-of-day workstation health snapshot

A final GNOME System Monitor snapshot showed a quiet, healthy idle state:

- CPU mostly idle, with only small per-thread activity;
- memory: 4.5 GB of 16.6 GB in use (26.9%);
- swap: 0 B of 8.6 GB in use;
- network essentially idle at the time of capture;
- disk I/O idle at the time of capture.

This snapshot is an operational observation only; the authoritative acceptance result remains `scripts/verify.sh`.

## End-of-day status

**CLOSED / ACCEPTED for 2026-09-17.**

The Fedora 44 / GNOME 50.4 workstation is in a zero-warning, zero-failure desired state, the Polish localization work is complete for the currently accepted extension set, and the repository documentation reflects the final validated baseline.
