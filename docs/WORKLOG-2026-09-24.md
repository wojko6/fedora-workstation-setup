# Worklog — 2026-09-24

## Scope

Today's Fedora work focused on turning the next engineering phase into a visible, issue-driven roadmap and revising the planned desktop-environment feature.

No KDE Plasma implementation was performed today. The accepted workstation baseline remains:

```text
Fedora 44
GNOME Shell 50.5
Wayland
PASS=256 WARN=0 FAIL=0 SKIP=0
VERIFY_RC=0
```

## Public engineering roadmap

A public GitHub Project named:

```text
Fedora Workstation — Engineering Roadmap
```

was configured for the Fedora repository.

Workflow:

```text
Backlog -> In Progress -> Validation -> Done
```

Project fields:

```text
Priority:
P1 — High
P2 — Medium
P3 — Low

Task Type:
Validation
Investigation
Maintenance
Feature
Tracker

Workstream:
Security
Networking
Desktop Environments
Packages
Automation
Backup / DR
Tailscale
Documentation
Project
```

The Project uses automatic transitions for newly added items, linked pull requests, closed/reopened issues and merged pull requests. Auto-archive remains disabled so completed portfolio history stays visible.

## Fedora Issues

The roadmap currently tracks:

```text
#11 Re-audit trusted Wi-Fi and FedoraWorkstation firewalld policy
#12 Design reproducible GNOME + KDE Plasma desktop environments
#13 Maintain Fedora major-upgrade readiness and validation workflow
#14 Maintain package, GNOME extension and localization pins
#15 Evaluate whether VSCodium localization should enter project scope
#16 Plan LUKS introduction for a future controlled reinstall or restore
#17 Fedora workstation engineering roadmap tracking
```

At the roadmap-setup checkpoint, active P1 work was:

```text
#11 — In Progress
#17 — In Progress
```

Later the same day, Issue #11 completed its physical acceptance gate and was closed; Issue #17 remains the umbrella roadmap tracker.

Issue #12 remains:

```text
Status: Backlog
Priority: P2 — Medium
Task Type: Feature
Workstream: Desktop Environments
```

## Desktop-environment plan revision

The earlier concept of maintaining two GNOME visual profiles was superseded by a stronger engineering target:

1. preserve the current accepted GNOME workstation as the canonical baseline;
2. add KDE Plasma later as an optional, reproducible alternate desktop session.

The first implementation phase must not replace GNOME or change the display manager at the same time.

The future validation scope includes:

- GNOME -> KDE -> GNOME session switching;
- rollback to the accepted GNOME baseline;
- XDG portals;
- MIME and default-application behavior;
- keyring/KWallet behavior;
- autostart interactions;
- Wayland session behavior;
- networking and Tailscale;
- audio and Bluetooth;
- removable storage;
- reproducible KDE configuration capture and restore;
- full repository verifier execution after the change.

Until those checks pass, KDE Plasma is a planned feature only and is not part of the accepted Fedora desired state.

## Documentation reconciliation

The repository documentation was reconciled with the roadmap:

- root `README.md` now distinguishes the accepted GNOME baseline from the planned GNOME + KDE Plasma feature;
- `PROJECT-STATUS.md` records the planned desktop-environment expansion without changing the accepted baseline;
- `docs/README.md` indexes this worklog and links the new Issue #12 scope;
- the Workstream name used by the public Project is `Desktop Environments`.

## End state

The Fedora project remains on the same accepted physical baseline. Today's changes are project-management and documentation changes only.

The next desktop-environment implementation should begin only after its package/session scope and rollback path have been defined and reviewed.


---

## Firewalld / Tailscale isolation finding

Issue #11 was advanced with a live read-only audit followed by a bounded A/B hardening test.

### Initial state

The trusted Wi-Fi interface remained correct:

```text
wlp4s0 -> workstation-kdeconnect

services:
dhcpv6-client
kdeconnect
mdns

forward: no
masquerade: no
explicit ports: none
rich rules: none
```

The broader default zone remained:

```text
FedoraWorkstation (default)

services:
dhcpv6-client
samba-client
ssh

ports:
1025-65535/udp
1025-65535/tcp

forward: yes
```

The Tailscale interface had no explicit firewalld zone:

```text
tailscale0 -> no zone
```

Tailscale itself was operating with:

```text
nf=on
```

The nftables inspection showed the Tailscale chains running before firewalld, while unmatched interface traffic still reached firewalld's default-zone dispatch.

### Listener correlation

Local listener inspection confirmed that SSH was not actually running, but several high-port listeners existed.

Relevant examples included:

```text
GSConnect / gjs:
TCP/1716
UDP/1716

Steam:
TCP/27036
UDP/27036
```

OpenSSH server units were absent/inactive and no TCP/22 listener was present.

### Remote baseline

From a separate Windows Tailnet peer, the Fedora Tailscale address was scanned:

```text
22/tcp      closed
1716/tcp    open
27036/tcp   open
```

This confirmed that the broad fallback policy was not merely theoretical: services bound on high ports were reachable through the Tailnet path.

### Runtime A/B test

A temporary runtime assignment placed `tailscale0` in firewalld's restrictive `block` zone.

Fedora retained outbound Tailscale connectivity:

```text
tailscale ping desktop-rrbnrq3
-> pong
```

The same Windows scan changed to:

```text
22/tcp      filtered
1716/tcp    filtered
27036/tcp   filtered
```

This established a causal before/after relationship between the broad fallback and remote exposure.

### Permanent exact-state design

A dedicated zone was then created:

```text
workstation-tailscale
target: DROP
interfaces: tailscale0
services: none
ports: none
protocols: none
forward: no
masquerade: no
rich rules: none
```

After `firewall-cmd --reload`:

```text
wlp4s0     -> workstation-kdeconnect
tailscale0 -> workstation-tailscale
tailscale ping Windows -> PASS
```

The remote Windows scan remained fully filtered for the three tested ports.

### Reboot persistence

The Fedora workstation was rebooted.

Post-reboot validation confirmed:

```text
wlp4s0     -> workstation-kdeconnect
tailscale0 -> workstation-tailscale
tailscale status -> healthy peer visibility
tailscale ping Windows -> PASS
22/tcp, 1716/tcp, 27036/tcp -> filtered from Windows
```

The unrelated Tailscale health note about peers advertising routes while `--accept-routes=false` remains expected because subnet-route acceptance is intentionally disabled in the current client configuration.

### Repository implementation

The manually validated state is promoted into reproducible desired state through:

```text
network/tailscale-firewall-zone.sh
install.sh
scripts/verify-security-posture.sh
scripts/test_tailscale_firewall_policy.py
scripts/test_verify_security_posture.py
scripts/check-static.sh
```

The implementation preserves the broader `FedoraWorkstation` fallback zone rather than rewriting it globally. The hardening is scoped specifically to `tailscale0`.

The repository implementation was subsequently verified on the physical workstation. After a portability fix for runtime firewalld target detection, the dedicated security posture verifier completed at `PASS=62 WARN=0 FAIL=0 SKIP=0`. The final full workstation verifier completed at `PASS=256 WARN=0 FAIL=0 SKIP=0`, `VERIFY_RC=0`, so the dedicated Tailscale zone is part of the accepted 2026-09-24 baseline.


---

## GSConnect / ddterm v73 localization maintenance

The 2026-09-24 maintenance cycle also re-audited two extensions that had independently advanced from v72 to v73 on the physical workstation.

### GSConnect v73

The exact v73 audit found 12 untranslated Polish catalog entries. Physical visual testing then exposed two additional user-visible plugin strings that were present in source but missing from the upstream POT/catalog. A later RunCommand audit found that the command-editor GtkBuilder resource lacked the correct gettext domain and that five factory command names were stored as English GSettings data rather than gettext strings.

The accepted repository implementation therefore includes:

- a 20-entry managed Polish catalog;
- Shell and preferences gettext-domain fixes;
- reviewed RunCommand editor translations;
- safe localization of the five factory display names:
  `Zablokuj`, `Wyloguj`, `Wyłącz`, `Uruchom ponownie`, `Uśpij`;
- preservation of the underlying command lines without translation;
- exact-version pristine backups and reconstruction-based verification.

Final GSConnect v73 extension-tree SHA-256:

```text
29bd02e3021fea0146311a623c3cd937dd96b224c41aeb83449d530f3468bffa
```

### ddterm v73

The exact v73 Polish catalog contained 10 fuzzy and 8 untranslated entries. The repository now carries an 18-entry completion overlay plus the version-pinned localized metadata description.

Final ddterm v73 extension-tree SHA-256:

```text
dbd72752dcc7687ab7a14a36ff1780f65e0b75e71245b8fa473e833b229c0d26
```

Both extensions passed dedicated physical verifiers and visual acceptance.

## Final day-close acceptance

The first complete physical verifier after the v73 promotion exposed three independent issues:

1. a runtime Tailscale target-reading portability bug in the verifier;
2. a transient Space Bar localization verification failure that did not reproduce in the dedicated verifier;
3. Dhruva live dock drift caused by an extra `org.gnome.SystemMonitor.desktop` entry.

The Tailscale verifier was fixed and its static tests passed. Space Bar then passed its dedicated exact-version verifier. Dhruva was restored to the repository desired state rather than promoting the incidental live drift.

The final full physical verifier completed with:

```text
PASS=256 WARN=0 FAIL=0 SKIP=0
VERIFY_RC=0
```

PR #21 was squash-merged to `main` as:

```text
67950750d3e4eb4c7005d0d9c5efdf8098edd303
```

Issue #11 was closed as completed. Issue #14 remains open as the recurring package/extension/localization maintenance tracker. The accepted workstation baseline at day close is Fedora 44 / GNOME Shell 50.5 / Wayland with the v73 localization pins and the dedicated `workstation-tailscale` DROP policy included.


## Ptyxis visual localization closure

Manual inspection of Ptyxis 50.1 on the physical Fedora 44 / GNOME Shell 50.5 workstation first confirmed the terminal context menu, search bar/history placeholder, search options, Set Title dialog, and built-in terminal inspector in Polish. Subsequent checks exposed launcher, Preferences/Profile/Shortcuts, Custom Links, palette-preview, and dynamic-label gaps; these were all folded into the same controlled PR #24 remediation rather than being accepted piecemeal.


### Ptyxis GNOME Shell launcher-action remediation

The launcher-level gap was traced to the RPM-owned `/usr/share/applications/org.gnome.Ptyxis.desktop`. The installed package is `ptyxis-50.1-2.fc44.x86_64`; `rpm -V` confirmed that the file was pristine before remediation, with SHA-256:

```text
8c596c2aff40ac062f61e6c541f0bccfa62091ce8503d63e2306d2e0a97f2b88
```

The audited desktop file provides localized action names for many languages but no `Name[pl]` for the three actions exposed by GNOME Shell. PR #24 adds a deterministic patcher that inserts only:

```text
New Window  -> Nowe okno
New Tab     -> Nowa karta
Preferences -> Preferencje
```

The action commands remain unchanged:

```text
ptyxis --new-window
ptyxis --tab
ptyxis --preferences
```

The installer keeps a pristine backup and refuses unknown package builds, fingerprint drift, changed action names/commands, or an unexpected pre-existing Polish action label. The dedicated verifier reconstructs the expected desktop file byte-for-byte from the pristine backup. Static fixtures cover normal patching and the fail-closed cases.

A subsequent complete resource audit of the Ptyxis preferences/profile/shortcut UI found **189 unresolved resource occurrences representing 152 unique msgids**. The gaps span Appearance/Behavior settings, scrolling, accessibility, bell/cursor behavior, keyboard shortcuts, profile editing, container/network settings, and shortcut-edit dialogs. The repository catalog now contains reviewed Polish translations for all 152 unique gaps found by the first complete resource scan. A later visual check of Custom Links exposed a seventh embedded resource, `ptyxis-custom-link-editor.ui`, with five additional untranslated strings, plus the C-generated gettext label `Add Link`. Six more unique translations were added, initially bringing the audit-cycle completion to 158 unique strings. Follow-up screenshots then exposed `Add Profile` and `Show Fewer Palettes`; exact Ptyxis 50.1 source review also identified the related dynamic `Select Font` string, and the palette-preview resource marked its English pangram as translatable. These four additions bring the audit-cycle completion to 162 unique strings. `ptyxis_resource_audit.py` now extracts eight exact embedded resources and requires every translatable resource msgid to be present in the installed Polish catalog; only `Terminal`, `Control-H`, and `ASCII DEL` are accepted as reviewed identity translations. The four C-generated labels (`Add Link`, `Add Profile`, `Show Fewer Palettes`, `Select Font`) are smoke-tested separately through gettext. Installer and verifier both enforce the expanded coverage.

The final expanded Ptyxis state was installed on the physical workstation, the dedicated verifier passed with zero unresolved audited resource strings, and the newly covered Preferences/Profile/Shortcuts/Custom Links/palette controls plus GNOME Shell launcher actions were visually confirmed in Polish. A final full repository verifier then completed at `PASS=256 WARN=0 FAIL=0 SKIP=0`, `VERIFY_RC=0`. Ptyxis 50.1 is fully technically and visually accepted.
